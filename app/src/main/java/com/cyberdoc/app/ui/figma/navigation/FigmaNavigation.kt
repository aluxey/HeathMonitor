package com.cyberdoc.app.ui.figma.navigation

enum class RootStage { ONBOARDING, HEALTH_CONNECT, APP }
enum class AppTab { HOME, GOALS, PROFILE }
enum class Overlay { NONE, SUMMARY, MANUAL, METRIC }
enum class Period { WEEK, MONTH }
